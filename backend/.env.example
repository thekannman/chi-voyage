# Outscraper API credentials
OUTSCRAPER_API_KEY=your_outscraper_api_key

# Supabase credentials
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key

# Application settings
NODE_ENV=development  # Set to 'production' to enable scheduled syncs
SYNC_INTERVAL=0 0 * * *  # Cron expression for sync schedule (default: daily at midnight) 